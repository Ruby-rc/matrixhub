apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Offline snapshot rendered from llm-d commit
  # 7029aac48505752dd51344ce552acc81b0deb774.
  - llm-d-modelserver.yaml
  - modelserver-service.yaml

patches:
  - path: patch-single-replica.yaml
